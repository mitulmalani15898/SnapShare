import Login from "../../components/Login";
import AuthWrapper from "../AuthWrapper";

const LoginPage = () => {
  return (
    <AuthWrapper>
      <Login />
    </AuthWrapper>
  );
};

export default LoginPage;
